{
  "name": "bank-app-api",
  "version": "1.0.0",
  "description": "server and api to the banking app",
  "main": "server.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "docker-compose up -d mongodb_container && nodemon server.mjs"
  },
  "author": "sarah w su",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.20.2",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
